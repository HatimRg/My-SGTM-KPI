# Only exclude dependency directories (installed via composer install / npm install)
backend/vendor/
frontend/node_modules/

# Keep environment files private
.env
.env.local

# Operating system files
.DS_Store
Thumbs.db
